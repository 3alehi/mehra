import React from 'react';
import ProfileLayout from '../ProfileLayout';

const page: React.FC = () => {
    return (
        <div>
                    <ProfileLayout>
                        
            address
                        </ProfileLayout>

        </div>
    );
};

export default page;